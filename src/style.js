const rules = {
  /**
   * single quotes.
   */
  'quotes': ['error', 'single'],
  
  /**
   * Enforces a tab width of 2
   */
  'indent': ['error', 2],

  /**
   * Require use of semicolons
   */
  'semi': [1, 'always'],
};

module.exports = { rules };